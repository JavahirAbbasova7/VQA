train_file: ['/hitvarth-vol/data/finetune_subset.json']
             
test_file: ['/hitvarth-vol/data/val_subset.json']
answer_list: '/hitvarth-vol/data/val_ann_subset.json'

# vqa_root: '/export/share/datasets/vision/VQA/Images/mscoco/' #train2014/
# vg_root: '/export/share/datasets/vision/visual-genome/'  #image/
vg_root: ''

vqa_root: '/hitvarth-vol/data/'

image_res: 224
batch_size_train: 16 
batch_size_test: 16 
k_test: 128

alpha: 0.4
distill: True
warm_up: True

eos: '[SEP]'

bert_config: './configs/config_bert.json'

optimizer: {opt: adamW, lr: 0.00002, weight_decay: 0.02}
schedular: {sched: cosine, lr: 0.00002, epochs: 8, min_lr: 0.000001, decay_rate: 1, warmup_lr: 0.00001, warmup_epochs: 4, cooldown_epochs: 0}
